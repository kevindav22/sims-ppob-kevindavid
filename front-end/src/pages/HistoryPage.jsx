import HistoryTransaction from '../components/History';

const HistoryPage = () => {
  return (
    <section className="animate-in fade-in duration-500">
      <HistoryTransaction />
    </section>
  );
};

export default HistoryPage;